export const metadata = {
  title: "SightLine",
  description: "Strategy-Driven Trading Signals"
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
